import { RemoneQuotesPipe } from './remove-quotes.pipe';

describe('RemoneQuotesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoneQuotesPipe();
    expect(pipe).toBeTruthy();
  });
});
