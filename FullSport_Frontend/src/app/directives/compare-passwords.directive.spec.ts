import { ComparePasswordsDirective } from './compare-passwords.directive';

describe('ComparePasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new ComparePasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
